# D2RSO Skill Icons

Drop skill icon files in this folder.

- Supported extensions: `.png`, `.jpg`, `.jpeg`, `.bmp`, `.gif`, `.webp`, `.ico`
- The Python registry loads files from this directory by default.
